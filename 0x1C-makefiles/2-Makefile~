CC = gcc
SRC = main.c school.c
OBJ = ${SRC:.c=.o}
NAME = school

${NAME}: ${OBJ}
	${CC} -o ${NAME} ${OBJ}
all: ${NAME}

main.o: main.c
	${CC} -c main.c -o main.o
%.o: %.c
	${CC} -c $< -o $@
clean:
	rm -f ${NAME} ${OBJ}
